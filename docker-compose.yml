version: '3'
services:
  app:
    build: gcitsonam/epharmacist:1.0.0
    ports:
      - "8089:8080"
    command: mvn clean spring-boot:run
    depends_on:
      - mysql
      
  mysql:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=epharmacy@123
      - MYSQL_DATABASE=epharmacy_db
      - MYSQL_USER=DevOps
      - MYSQL_PASSWORD=devops@123
    ports:
      - "3307:3306"
    container_name: mysql
    expose:
      - '3306'
      
